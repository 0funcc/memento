// displays tasks that are due today

import SwiftUI

struct TodayView: View {
    var body: some View {
        NavigationStack {
            VStack {
                
            }
            .navigationTitle("Today")
        }
    }
}

#Preview {
    TodayView()
}
