// displays all tasks

import SwiftUI

struct AllView: View {
    var body: some View {
        NavigationStack {
            VStack {
                
            }
            .navigationTitle(Text("All Tasks"))
        }
    }
}

#Preview {
    AllView()
}
